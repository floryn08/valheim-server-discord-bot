export type Config = {
  token: string;
  clientId: string;
  guildIds: string;
  deploymentName: string;
  namespace: string;
  serverName: string;
  joinCodeLoopCount: number;
  joinCodeLoopTimeoutMillis: number;
};
